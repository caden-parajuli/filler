# TODO

Automatically include JS/CSS files in the HTML based on the nakefile. This could be done by passing -d:cssFile="index" or something similar, but that's kind of inelegant and creates directory issues.